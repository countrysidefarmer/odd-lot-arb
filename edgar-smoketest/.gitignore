# Local SEC contact (User-Agent) — personal, never committed.
.env

# Regenerable / bulky artifacts — not committed.
# (git does NOT allow trailing comments on a pattern line, so keep them separate)
# cache/ is ~1.3GB of fetched filings + data.sec.gov JSON; rebuilt on run.
cache/
# deduped index lists, regenerated by step1/run
filings.json
filings_5d.json
filings_test.json
run_full.log
__pycache__/
*.pyc
# Generated report outputs (regenerated every run — don't track, avoids churn)
form4_purchases.csv
sc13d_filings.csv
signal.md
shortlist.md
screens.md
data_quality.json
